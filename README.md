# Hospital Patient Record Management System

Run locally:

1. Create virtualenv and install requirements:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

2. Start MongoDB locally or set `MONGO_URI` env var.

3. Run the app:

```bash
python app.py
```
